begin int fibonacci(int v1):
  begin if(v1 == 0):
    return 0;
  end if;
  begin if(v1 == 1):
    return 1:
  end if;
  return fibonacci(v1-2) + fibonacci(v1-1);
end fibonacci;


begin print_list(int[] list):
  begin foreach int i in list:
    println(i);
  end foreach;
end print_list;

begin struct Aluno:
  string nome;
  int idade;
  real media;
end Aluno;

begin program main:
  int valor
  Aluno a = create Aluno("Jonas", 12, 7.4);
  valor = 10;
  print(valor);
  valor = fibonacci(valor);
  print (valor);
  int[] l = {1, 2, 3, 4};
  print_lista(l);
  print("Nome: " + a.nome + "Idade: " + a.idade + "Media: " + media);
end program;